#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                selenium-side-runner C:/Tests/test_jenkins.side
            }
        }
        stage("Second step") {
            steps {
                echo("2 steeeep")
            }
        }
    }
}
